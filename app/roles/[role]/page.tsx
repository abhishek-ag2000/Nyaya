import { BackLink } from "@/components/BackLink";
import { Disclaimer, Footer, Header } from "@/components/SiteChrome";
export default async function RolePage({ params }: { params: Promise<{ role: string }> }) { const route = await params; const role = route.role.split("-").map((part) => part[0]?.toUpperCase() + part.slice(1)).join(" "); return <><Disclaimer /><Header /><main className="wrap static-page"><p className="kicker">Mock role dashboard</p><h1>{role}</h1><p>This placeholder shows where a role-specific workspace would begin. No role data, permissions, or live services are connected on this site.</p><div className="case-section"><h2>Designed view</h2><p>A simple, role-appropriate summary of active matters, tasks, and relevant documents would appear here using demo data.</p></div><p><BackLink href="/">Return home</BackLink></p></main><Footer /></>; }
