import { FileSearch, Gavel, ListChecks, Search, ScrollText } from "lucide-react";
import Link from "next/link";
const items=[{title:"CNR Number",description:"Search a synthetic case ID",icon:Search,href:"/find-case?mode=cnr"},{title:"Case Status",description:"Find a case by party or advocate",icon:FileSearch,href:"/find-case?mode=status"},{title:"Court Orders",description:"View local synthetic orders",icon:Gavel,href:"/find-case?mode=orders"},{title:"Daily Cause List",description:"Select a court and view listed matters",icon:ListChecks,href:"/cause-list"},{title:"Caveat Search",description:"Prototype search reference",icon:ScrollText,href:"/find-case?mode=caveat"}];
export default function FindCaseMenu(){return <section className="find-case-menu"><div className="wrap"><div className="find-menu-heading"><p className="kicker">Find my case</p><h2>Choose how you want to begin.</h2><p>All searches use bundled synthetic prototype data only.</p></div><div className="find-menu-grid">{items.map(({title,description,icon:Icon,href})=><Link href={href} key={href}><span className="find-menu-icon"><Icon aria-hidden="true"/></span><div><h3>{title}</h3><p>{description}</p></div><b aria-hidden="true">→</b></Link>)}</div></div></section>}
