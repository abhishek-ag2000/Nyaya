"use client";
import Link from "next/link";
import { useEffect, useState } from "react";
import { getMockRole } from "@/data/mock-session";
import { roleConfig, type Role } from "@/data/roles";
import { demoUnifiedCase, type UnifiedCase } from "@/data/unified-case";
import { loadDemoCase } from "@/data/demo-case-store";
const notes: Record<Role,{name:string; description:string; access:string[]; preference:string}> = { citizen:{name:"Citizen Demo",description:"Follow case updates and next steps in plain language.",access:["Tracked cases","Case updates","Documents shared with you"],preference:"plain-language case updates"},advocate:{name:"Advocate Demo",description:"Manage local matter activity, filing review, and procedural dates.",access:["Case portfolio","Filing review","Hearing calendar"],preference:"filing and hearing reminders"},judge:{name:"Judge Demo",description:"Administrative case context only; judicial decisions remain entirely human.",access:["Docket overview","Document readiness","Court information"],preference:"docket and readiness updates"},registry:{name:"Registry Staff Demo",description:"Access to filing intake and scrutiny context.",access:["Intake queue","Scrutiny context","Registry handoff"],preference:"scrutiny queue notifications"},stenographer:{name:"Stenographer Demo",description:"Review proceeding and transcript-draft context.",access:["Proceedings","Draft review","Linked documents"],preference:"proceeding updates"},police:{name:"Investigation Demo",description:"Authorised court-linkage and document-status context.",access:["Linked matters","Court dates","Authorised documents"],preference:"court-linkage updates"}};
export default function ProfileSettings(){const [role,setRole]=useState<Role|null>(null),[updates,setUpdates]=useState(true),[caseData,setCaseData]=useState<UnifiedCase>(demoUnifiedCase);useEffect(()=>{const refresh=()=>{setRole(getMockRole());setCaseData(loadDemoCase(demoUnifiedCase.id))};refresh();window.addEventListener("nyaya-mock-session",refresh);window.addEventListener("nyaya-demo-case-updated",refresh);return()=>{window.removeEventListener("nyaya-mock-session",refresh);window.removeEventListener("nyaya-demo-case-updated",refresh)}},[]);const info=role?notes[role]:{name:"Guest Demo",description:"Log in to choose a role experience.",access:["Public case search","Nyaya Guide"],preference:"guide updates"};const workspace=role?roleConfig[role]:null;return <main className="wrap static-page profile-page"><p className="kicker">Profile settings · mock only</p><h1>My profile</h1><p>Local demo preferences and role context. Nyaya does not request, store remotely, or verify personal, identity, or contact information.</p><section className="profile-card profile-overview"><div><span>Mock profile</span><h2>{info.name}</h2><p>{info.description}</p></div><dl><div><dt>Signed in as</dt><dd>{workspace?.label??"Guest"}</dd></div><div><dt>Workspace</dt><dd>{workspace?.workspace??"Public access"}</dd></div><div><dt>Session data</dt><dd>Local browser only</dd></div></dl></section>{workspace&&<Link className="outline-cta" href={workspace.home}>Open {workspace.workspace} →</Link>}<div className="profile-grid"><section className="profile-card"><span className="eyebrow">Role access</span><h2>{workspace?.workspace??"Public access"}</h2><ul className="profile-list">{info.access.map(item=><li key={item}>✓ {item}</li>)}</ul><p className="profile-note">Role access is visual-only and does not grant real institutional access.</p></section><section className="profile-card"><span className="eyebrow">Preferences</span><h2>Local settings</h2><label className="profile-toggle"><input checked={updates} onChange={(event)=>setUpdates(event.target.checked)} type="checkbox"/> Show {info.preference}</label><label className="profile-toggle"><input defaultChecked type="checkbox"/> Use reduced visual detail in notices</label><p className="profile-note">Preferences remain in this browser only and do not enable real notifications.</p></section></div><section className="profile-card profile-documents"><span className="eyebrow">My documents</span><h2>{role==="citizen"?"Documents shared with you":"Documents in your shared case workspace"}</h2><p>{caseData.title} · {caseData.documents.length} local document{caseData.documents.length===1?"":"s"}</p><div>{caseData.documents.map(document=><article key={document.id}><div><b>{document.title}</b><small>{document.category} · {document.pages} pages · Uploaded by {document.addedBy}</small></div><span>{document.processing?.status.replace("-"," ")??"available"}</span></article>)}</div></section><section className="profile-card"><span className="eyebrow">Account safety</span><h2>What Nyaya does not use</h2><p>No Aadhaar, PAN, real OTP, payments, health data, live court accounts, or government systems are used on this site.</p></section></main>}
